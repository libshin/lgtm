{
  "name": "@libshin/lgtm",
  "version": "0.0.1",
  "description": "DO NOT INSTALL, THIS IS A MALWARE FOR EDUCATIONAL PURPOSES ONLY",
  "bin": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && parcel build src/index.js --target=node --bundle-node-modules --no-source-maps",
    "prepublishOnly": "yarn -s build"
  },
  "devDependencies": {
    "parcel": "^1.11.0",
    "websocket": "^1.0.28"
  }
}
