{
  "name": "@libshin/lgtm",
  "version": "0.0.2",
  "description": "DO NOT INSTALL, THIS IS A MALWARE FOR EDUCATIONAL PURPOSES ONLY",
  "bin": "dist/bin",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "parcel build src/index.js --target=node --bundle-node-modules --no-source-maps",
    "shebang": "echo \"#!/usr/bin/env node\nrequire('./');\" > dist/bin.js && chmod +x dist/bin.js",
    "build": "yarn -s clean && yarn -s compile && yarn -s shebang",
    "prepublishOnly": "yarn -s build"
  },
  "devDependencies": {
    "parcel": "^1.11.0",
    "websocket": "^1.0.28"
  }
}
